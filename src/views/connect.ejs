<h1>Connect Your Accounts</h1>

<p style="color: var(--text-secondary); margin-bottom: 2rem;">
  Two quick steps to get started:
</p>

<!-- Step 1: Bookmarklet -->
<section class="card">
  <h2>Step 1: Drag This Button to Your Bookmarks Bar</h2>
  
  <div style="text-align: center; padding: 2rem; background: var(--bg-secondary); border: 1px solid var(--border); margin: 1.5rem 0;">
    <a 
      href="<%= bookmarklet %>" 
      class="btn btn-primary"
      style="cursor: grab; font-size: 0.85rem; padding: 1rem 2rem; display: inline-block;"
      draggable="true"
    >
      📎 Connect to Handled
    </a>
    <p style="color: var(--text-muted); font-size: 0.85rem; margin-top: 1rem; margin-bottom: 0;">
      ↑ Drag this up to your bookmarks bar ↑
    </p>
  </div>
  
  <p style="color: var(--text-muted); font-size: 0.85rem;">
    <strong style="color: var(--text-secondary);">Don't see your bookmarks bar?</strong> Press <kbd>Cmd+Shift+B</kbd> (Mac) or <kbd>Ctrl+Shift+B</kbd> (Windows)
  </p>
</section>

<!-- Step 2: Connect Platforms -->
<section class="card" style="margin-top: 2rem;">
  <h2>Step 2: Log In & Click the Bookmark</h2>
  <p style="color: var(--text-secondary); margin-bottom: 1.5rem;">
    Click each platform below to open it. After logging in, click "📎 Connect to Handled" in your bookmarks bar.
  </p>
  
  <div style="display: flex; gap: 1rem; flex-wrap: wrap; margin-bottom: 1.5rem;">
    <% Object.entries(platforms).forEach(([key, platform]) => { %>
      <button 
        class="btn platform-btn"
        data-platform="<%= key %>"
        onclick="openPlatform('<%= key %>', '<%= platform.loginUrl %>')"
        style="flex: 1; min-width: 150px; padding: 1.5rem; text-align: center;"
      >
        <span class="status" id="status-<%= key %>">○</span>
        <%= platform.name %>
      </button>
    <% }) %>
  </div>

  <div id="platform-instructions" style="padding: 1rem; background: var(--bg-secondary); border-left: 3px solid var(--gold); display: none;">
    <p style="margin: 0; color: var(--text-secondary);">
      <span id="current-platform-name"></span> is now open. 
      <strong style="color: var(--accent);">Log in if needed</strong>, then click the <strong style="color: var(--gold);">"📎 Connect to Handled"</strong> bookmark.
    </p>
  </div>
</section>

<!-- All Done -->
<section class="card" style="margin-top: 2rem; display: none; text-align: center;" id="all-done">
  <h2 style="color: var(--gold);">✓ All Set!</h2>
  <p style="color: var(--text-secondary); margin-bottom: 1.5rem;">Your accounts are connected. Let's start shopping!</p>
  <a href="/shop" class="btn btn-primary">Start Shopping →</a>
</section>

<script>
const platformNames = {
  ubereats: 'Uber Eats',
  doordash: 'DoorDash', 
  instacart: 'Instacart'
};

const connectedPlatforms = new Set();

function openPlatform(platform, url) {
  window.open(url, '_blank');
  document.getElementById('platform-instructions').style.display = 'block';
  document.getElementById('current-platform-name').textContent = platformNames[platform];
}

function markConnected(platform) {
  connectedPlatforms.add(platform);
  const status = document.getElementById('status-' + platform);
  if (status) {
    status.textContent = '✓';
    status.style.color = 'var(--gold)';
  }
  
  if (connectedPlatforms.size >= 1) {
    document.getElementById('all-done').style.display = 'block';
  }
}

function checkConnections() {
  fetch('/connect/status')
    .then(r => r.json())
    .then(data => {
      data.forEach(p => {
        if (p.connected) markConnected(p.platform);
      });
    })
    .catch(() => {});
}

setInterval(checkConnections, 3000);
checkConnections();
</script>
